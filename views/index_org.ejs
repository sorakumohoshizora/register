<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <h1><%= title %></h1>
    <p>Welcome to <%= title %></p>
    <% if (data != null) { %>
        <table border="1">
        <% for(var i=0; i < data.length; i++){ %>
            <tr>
            <td width="200px"><%= data[i].user_id %></td>
            <td width="200px"><%= data[i].user_name %></td>
            <td width="300px"><%= data[i].password %></td>
            </tr>
        <% } %>
        </table>
    <% } %>
  </body>
</html>
